import Teclado from './Teclado';
import { render, screen } from '@testing-library/react' 


/* test('quando a tecla ENTER é clicada, a palavra só será verificada se ela tiver exatamente 5 letras e fazer parte do banco de palavras', () => {
    
    render(<Teclado />);

    const teclas = screen.getAllByRole('button');

    expect(teclas).toBeInTheDocument;

}) */